字符串类：
String：不可变类，不可原地（内存地址不变即同一个对象）修改，改变字符串必须是新的String对象，字符串常量池，当确定的即使用双引号引起来的赋值String新对象时，会先在字符串常量池中查找有没有相同的字符串，有则将该字符串的引用赋值这个新String对象（此时有多个对象使用共同的字符串常量池中String对象引用），没有就将其加入字符串常量池。
StringBuffer：可变字符串对象，同步，效率较好。（对字符串进行大量操作时建议使用）
StringBuilder：可变字符串对象，不同步，效率更好。
